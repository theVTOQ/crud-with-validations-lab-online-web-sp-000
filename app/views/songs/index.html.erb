<h1>All Songs:</h1>
<ul>
  <% @songs.each do |song| %>
    <li>
      <a href="/songs/<%= song.id %>">"<%= song.title %>" by <%= song.artist_name %> (<%= song.release_year %>)</a>
    </li>
  <% end %>
</ul>
